# Structural version (C++)

Console application in **structural style** (functions + headers, no OOP).

## Build and run

```bash
g++ -std=c++17 src/main.cpp src/number_utils.cpp -o lab4_struct
./lab4_struct
```
